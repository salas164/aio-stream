{
  "name": "@aiostreams/server",
  "version": "0.0.0",
  "private": true,
  "main": "dist/server.js",
  "description": "Combine all your streams into one server",
  "scripts": {
    "build": "tsc",
    "start": "node dist/server.js",
    "test": "vitest run --passWithNoTests",
    "test:watch": "vitest watch",
    "prepublish": "npm run build"
  },
  "dependencies": {
    "@aiostreams/core": "^0.0.0",
    "express": "^4.21.2",
    "express-rate-limit": "^7.5.0"
  },
  "devDependencies": {
    "@types/express": "^5.0.1",
    "@types/express-rate-limit": "^5.1.3",
    "@types/node": "^20.14.10",
    "typescript": "^5.6.3"
  }
}
